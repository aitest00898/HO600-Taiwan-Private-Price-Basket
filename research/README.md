# Research area

Research branches and issues are intentionally non-canonical. Their counts must never be presented as formal HO600 progress until the corresponding workbook has passed export, re-import, validation, and merge-to-main gates.
